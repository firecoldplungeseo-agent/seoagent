node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Generated reports — keep structure, ignore contents during dev
reports/*.json
reports/*.md
!reports/.gitkeep

# Generated keyword research output (seeds/ stays tracked — those are hand-curated)
keywords/research/
optimizations/*.md
optimizations/*.json
competitors/*-auto-*.md

# Weekly snapshots (state/) and digests must be committed so the scheduled agent
# can diff vs last week's run. They're not secrets — just data.
